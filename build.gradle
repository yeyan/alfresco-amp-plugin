apply plugin: 'groovy'

dependencies {
    compile gradleApi()
    compile localGroovy()
}

apply plugin: 'maven-publish'

repositories {
    mavenCentral()
}

dependencies {
    // test hasn't implemented yet
    // testCompile 'junit:junit:4.11'
}

group = 'com.github.amp'
version = '1.0-SNAPSHOT'

publishing {
    publications {
        maven(MavenPublication) {
            from components.java
        }
    }
    repositories {
        // change this to any repository you want publish
        mavenLocal()
    }
}
